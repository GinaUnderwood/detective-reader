[project]
name = "detective-reader"
version = "0.1.0"
description = "Detective Reader web and Azure Speech service"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
  "httpx==0.28.1",
  "uvicorn==0.51.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
